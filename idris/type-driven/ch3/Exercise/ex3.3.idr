import Data.Vect

transposeMat : Vect m (Vect n elem) -> Vect n (Vect m elem)


addMatrix : Vect m (Vect n elem) -> Vect m (Vect n elem) -> Vect m (Vect n elem)


multMatrix : Vect m (Vect n elem) -> Vect n (Vect p elem) -> Vect m (Vect p elem)


